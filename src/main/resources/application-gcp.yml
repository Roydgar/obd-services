spring:
  cloud:
    gcp:
      sql:
        instance-connection-name: obd-services:europe-west1:obd-services-sql-db
        database-name: obd-backend-db
  security:
    basic:
      enabled: false
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create